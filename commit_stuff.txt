git add test2.js
git commit -am "commit message" => commit local (only local, don't need a network connection, as opposed to svn)
git push origin master          => push on GITHUB 



other uses:

git add directory/
git add *
